### SDDM
  sddm run ot start pc
    sudo systemctl enable sddm
  sddm russian locale
    sudo sed -i 's|\[Service\]|\[Service\]\nEnvironment=LANG=ru_RU.UTF-8|' /etc/systemd/system/display-manager.service

